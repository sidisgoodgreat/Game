package Main;
package csaProject;
import java.util.Random;
private int Encounter_Type;
//we import from overworld.
public class Encounter {
    public Encounter(String area){
        Random rand = new Random();
        this.Encounter_Type = Encounter_Type;
        Encounter_Type = rand.nextInt(5);
        this.area = area;
        if (Encounter_Type == 1 && area == "Grassland") {
            
    }
}
